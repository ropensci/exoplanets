# parse_url works

    argument "format" is missing, with no default

---

    argument "format" is missing, with no default

---

    argument "format" is missing, with no default

---

    Expected formats are:
    * csv
    * tsv
    * json

